/**
 * Project-wide UI defaults. Toggle here to affect the whole app.
 */
export const uiSettings = {
  /** Hide browser spinner buttons on <input type="number"> */
  hideNumberInputSpinners: true,
} as const;
